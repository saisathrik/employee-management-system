PORT=5000
MONGO_URI=mongodb+srv://saisathrik05_db_user:Sai12345@cluster0.8kwmuo8.mongodb.net/employee_management?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# First admin account, created automatically on server start if no admin exists yet.
# All other employees and tasks are created dynamically through the app (or the
# /api/employees and /api/tasks endpoints) — nothing else is seeded.
ADMIN_NAME=Admin
ADMIN_EMAIL=admin@me.com
ADMIN_PASSWORD=admin123
